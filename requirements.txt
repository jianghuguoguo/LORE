# RefPenTest – 核心依赖
# 用于解析渗透测试代理日志并生成结构化 TurnSequence

# ── 核心依赖 ────────────────────────────────────────────────────────────────
pyyaml>=6.0
python-dateutil>=2.8.2
tqdm>=4.66.0

# ── Phase 3：LLM 集成依赖（OpenAI 兼容 SDK，支持 DeepSeek / OpenAI / Qwen 等） ──
openai>=1.0.0

# ── 测试依赖 ────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Phase 1-2：WeChat 爬取（APScheduler + SQLite，零外部服务依赖）────────────
# 定时调度（替代 Celery Beat）
apscheduler>=3.10

# UI 自动化（仅 Windows）
# uiautomation>=2.0.18  # pip install uiautomation
# pywin32>=306           # pip install pywin32

# HTTP 客户端（搜狗发现、社区同步）
httpx>=0.27

# HTML 解析（拦截器 + 搜狗解析 + RSS 全文提取）
beautifulsoup4>=4.12
lxml>=5.0

# ── Layer 4 ：知识库主动维护层 ──────────────────────────────────────────────
trafilatura>=1.8.0       # 网页正文提取（已安装）
# apscheduler 已在上方声明且已安装

# RSS/Atom Feed 解析
feedparser>=6.0

# SQLite 为 Python 内置，无需额外安装

# ── Phase 3（后续）：数据清洗 ────────────────────────────────────────────────
# datasketch>=1.6         # MinHash 近似去重：pip install datasketch
# mitmproxy>=10.0         # MITM 拦截器：pip install mitmproxy

# ── 可选：更快的 JSON 解析 ───────────────────────────────────────────────────
# orjson>=3.9.0

# ── 可选：类型检查 ────────────────────────────────────────────────────────────
# mypy>=1.5.0
# types-PyYAML>=6.0.0
